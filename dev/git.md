# Git

```aln
Command to store credentials (globally) next time 'git pull' is called : git config --global credential.helper store
Command to add a submodule (another Git repo) to the current project using its {url} : git submodule add url
```
